# Kinova Gen3 TeachUI Dependencies
# Note: kortex_api is not hosted on public PyPI. 
# It must be installed from a local wheel (.whl) file downloaded from Kinova.
# Example: pip install C:\path\to\kortex_api-2.X.X-py3-none-any.whl

# High-performance cross-platform audio playback engine (WAV/MP3)
miniaudio>=1.40.0

# Modern flat UI toolkit built on top of Tkinter
customtkinter>=5.2.0

# Python Imaging Library for loading and tinting interface icons
Pillow>=10.0.0

# Tkinter native SVG vector rendering support
tksvg>=0.5.6

# Async WebSockets library for local face web server
websockets>=12.0

# Kinova Gen3 Robotics API (Local wheel)
./kortex_api-2.8.0.post5-py3-none-any.whl
